package ${pacote}.web.response;

<#list imports as import>
import ${import};
</#list>

import lombok.Data;

@Data
public class  ${nome}Response {
 <#list atributos as atributo>
  private ${atributo.tipo} ${atributo.nome};
</#list>

}
