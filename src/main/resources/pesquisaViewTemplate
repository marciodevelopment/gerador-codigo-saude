package ${pacote}.entity.view;

<#list imports as import>
import ${import};
</#list>

import lombok.AllArgsConstructor;
import lombok.Getter;

@Getter
@AllArgsConstructor
public class ${nome}PesquisaView {
<#list atributos as atributo>
  private ${atributo.tipo} ${atributo.nome};
</#list>
 
}
